<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Where the camera app writes a just-captured photo before we read it back. -->
    <cache-path name="captured_images" path="images/" />
</paths>
